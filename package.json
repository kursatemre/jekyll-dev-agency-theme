{
  "name": "animasyonlu-kurumsal-web-sitesi",
  "version": "1.0.0",
  "description": "Modern Jekyll website with Tailwind CSS and animations",
  "scripts": {
    "css:build": "npx tailwindcss -i ./assets/css/main.css -o ./_site/assets/css/main.css --minify",
    "jekyll:build": "bundle exec jekyll build",
    "dev": "npm run css:build && bundle exec jekyll serve --livereload",
    "build": "npm run jekyll:build && npm run css:build",
    "clean": "bundle exec jekyll clean",
    "vercel-build": "npm run jekyll:build && npm run css:build"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "@vercel/analytics": "^1.6.1",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "tailwindcss-cli": "^0.1.2"
  }
}
